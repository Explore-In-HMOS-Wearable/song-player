export class ImageConstants {
  /**
   * The size of song image.
   */
  static readonly SONG_IMAGE_SIZE: number = 60;
  /**
   * The size of song image.
   */
  static readonly SONG_IMAGE_SIZE_LARGE: number = 400;

  /**
   * The top margin of song image.
   */
  static readonly SONG_IMAGE_RADIUS: number = 16;
}